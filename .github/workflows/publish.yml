name: Publish

on:
  release:
    types: [created]

jobs:
  publish:
    uses: matthieubosquet/workflows/.github/workflows/node-npm-publish.yml@v0.4.0
    secrets:
      token: ${{ secrets.NPM_TOKEN }}
